@model ERP_JOSEREIS.Models.Estado

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Estado</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.NomeEstado)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NomeEstado)
            @Html.ValidationMessageFor(model => model.NomeEstado)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Sigla)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Sigla)
            @Html.ValidationMessageFor(model => model.Sigla)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
