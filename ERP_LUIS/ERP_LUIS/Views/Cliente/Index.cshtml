@model IEnumerable<ERP_ANDERSON.Models.Cliente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Novo Cliente PF", "CreatePF")
    @Html.ActionLink("Novo Cliente PJ", "CreatePJ")
</p>
<table>
    <tr>
        <th>Nome</th>
        <th>Preferencial</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>@item.pessoa.Nome</td>
        <td>@item.Preferencial</td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdCliente }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdCliente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdCliente })
        </td>
    </tr>
}

</table>
